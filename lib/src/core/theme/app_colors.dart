import 'package:flutter/material.dart';

class AppColors {
  // static const Color primary = Color.fromARGB(255, 27, 115, 247);
  static const Color primary = Color.fromARGB(255, 0, 0, 0);
  static const Color secondary = Color.fromARGB(255, 119, 194, 255);
  static const Color background = Color.fromARGB(255, 255, 255, 255);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFB00020);

  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFF000000);
  static const Color onBackground = Color(0xFF000000);
  static const Color onSurface = Color(0xFF000000);
  static const Color onError = Color(0xFFFFFFFF);

  // Bottom Navigation
  static const Color selectedIcon = Color.fromARGB(255, 0, 0, 0);
  static const Color unselectedIcon = Color.fromARGB(255, 0, 0, 0);

  // Hint Text
  static const Color hintText = Colors.grey;
  static const Color hintIcon = Colors.grey;

  // Borders
  static Color borders = Colors.grey.shade300;
  static Color focusedBorders = Colors.black54;

  // Buttons
  static const Color uploadButton = Colors.deepOrangeAccent;
}
